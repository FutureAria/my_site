# Local development placeholders only.
# Do not commit real values in .env.local or .env.oracle.

# Preferred for production:
# Generate with: npm run admin:hash -- "your-admin-password"
ADMIN_PASSWORD_HASH=

# Legacy fallback for local development only.
ADMIN_PASSWORD=

# Optional contact form SMTP settings.
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
CONTACT_TO=
